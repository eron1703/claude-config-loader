# =============================================================================
# Shared Tools Environment Variables - dev-01 (65.21.153.235)
# =============================================================================
# PREPARATION CONFIG - Update values before deployment.
# Sensitive values should be stored in GitLab CI/CD variables and injected
# at deploy time, not committed to git.
# =============================================================================

# -----------------------------------------------------------------------------
# Server
# -----------------------------------------------------------------------------
SERVER_IP=65.21.153.235

# -----------------------------------------------------------------------------
# Plane - Project Management
# Docs: https://docs.plane.so/self-hosting
# -----------------------------------------------------------------------------

# Database (separate from FlowMaster PostgreSQL on 5432)
PLANE_DB_PASSWORD=CHANGE_ME_plane_db_password

# Secret key for Django (generate with: python -c "import secrets; print(secrets.token_hex(32))")
PLANE_SECRET_KEY=CHANGE_ME_plane_secret_key

# MinIO (S3-compatible storage for Plane file uploads)
MINIO_ROOT_USER=plane-minio
MINIO_ROOT_PASSWORD=CHANGE_ME_minio_password

# Plane API key (generated after first login, used for automation)
# Current key from handover: plane_api_bb5d084bdaae44c4b4610965fcfc0600
PLANE_API_KEY=plane_api_bb5d084bdaae44c4b4610965fcfc0600

# Plane workspace/project IDs (for API automation)
PLANE_WORKSPACE_SLUG=flowmaster
PLANE_PROJECT_ID=4704cb6d-9577-4761-b173-8a2bb279d00d

# Plane state IDs
PLANE_STATE_BACKLOG=079e8734-fba6-4bc4-8cf7-807b465408b6
PLANE_STATE_IN_PROGRESS=7c3c1ad1-8692-4947-859c-721fa17fe06c
PLANE_STATE_DONE=bd7b2b26-a29a-410e-94be-ae87299ca4b5

# -----------------------------------------------------------------------------
# Grafana - Monitoring Dashboards
# -----------------------------------------------------------------------------

# Admin password (store in GitLab CI/CD: GRAFANA_ADMIN_PASSWORD)
GRAFANA_ADMIN_PASSWORD=CHANGE_ME_grafana_admin_password

# -----------------------------------------------------------------------------
# Agent Chat / Telemetry
# -----------------------------------------------------------------------------
AGENT_CHAT_LOG_LEVEL=info
AGENT_CHAT_TELEMETRY_ENABLED=true

# -----------------------------------------------------------------------------
# GitLab CI/CD Variable Mapping
# -----------------------------------------------------------------------------
# The following values should be stored as GitLab CI/CD variables:
#
# Variable Name               | Type     | Scope  | Description
# ----------------------------|----------|--------|----------------------------------
# PLANE_DB_PASSWORD           | Variable | dev-01 | Plane PostgreSQL password
# PLANE_SECRET_KEY            | Variable | dev-01 | Django secret key
# MINIO_ROOT_PASSWORD         | Variable | dev-01 | MinIO admin password
# PLANE_API_KEY               | Variable | dev-01 | Plane API key for automation
# GRAFANA_ADMIN_PASSWORD      | Variable | dev-01 | Grafana admin password
# SSH_PRIVATE_KEY             | File     | dev-01 | SSH deploy key for dev-01
